let express,routes,morgan,helmet;_1de‍.w("express",[["default",["express"],function(v){express=v}]]);_1de‍.w("../routes",[["default",["routes"],function(v){routes=v}]]);_1de‍.w("morgan",[["default",["morgan"],function(v){morgan=v}]]);_1de‍.w("helmet",[["default",["helmet"],function(v){helmet=v}]]);




const app = express();

app.use(helmet());
app.use(morgan('combined'));
app.use(express.json({limit: '50mb'}));
app.use(express.urlencoded({extended: true, limit: '50mb'}));

app.use('/user', routes.user);

app.use((req, res) => {
    res.status(404).send('404 page not found');
});

app.listen(8080, () => {
    console.log('run');
});